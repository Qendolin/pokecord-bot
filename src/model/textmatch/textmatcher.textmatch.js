const dHash = require('./image/dhash.image.autocatch')

class TextMatcher {
	constructor(patternText) {}

	/**
	 * @param {string} text
	 * @returns {bool} Ture if `text` matches
	 */
	async match(text) {
		//TODO
		//render the text onto a canvas, then compare to patternText with dHash
	}
}
